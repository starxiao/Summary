
动态路由匹配


我们经常需要把某种模式匹配到的所有路由，全都映射到同个组件

/user/:id  对于不同id不相同的用户都要用User组件来渲染

在User组件内可以 调用this.$route.params


 /user/1 导航到 /user/2，原来的组件实例会被复用。因为两个路由都渲染同个组件，比起销毁再创建，复用则显得更加高效。不过，这也意味着组件的生命周期钩子不会再被调用。

 复用组件的时候，想对路由参数变化做出响应的话，可以使用watch $route对象变化

 wacth{
 	'$route':(to,from){

 	}
 }