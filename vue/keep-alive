

<keep-alive></keep-alive>

在动态组件上使用<keep-alive></keep-alive>,当在这些组件之间切换的时候，你有时会想保持这些组件的状态，以避免反复重渲染导致的性能问题。

也就是缓存组件，不会重新创建一个新的实例。

header.vue

<keep-alive>
	<router-view></router-view>	
</keep-alive>

当路由切换的时候，匹配到的组件之间的切换都会缓存组件，最直观的是不在执行beforeCreate、<mounted>等生命周期函数,data也不会改变

	
